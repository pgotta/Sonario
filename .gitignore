venv/
__pycache__/
*.pyc
cache/*.json
output/*
!output/.gitkeep
!cache/.gitkeep
credentials/credentials.json
credentials/token.json
credentials/api_keys.json
credentials/groq_usage.json
.DS_Store
history/
history/*.json

# Windows .bat launchers — kept local, documented in BUILD.md, never committed
*.bat

# Optional local LLM bridges (downloaded by their setup .bat, never committed)
Windows-Copilot-API/
Deepseek-API/
